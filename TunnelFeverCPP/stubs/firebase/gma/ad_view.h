#pragma once
// Stub — Firebase GMA ads are handled by the Swift/ObjC layer on iOS.
// The C++ engine includes this header but does not call any GMA API directly.
namespace firebase { namespace gma {} }
